name: Keep Veritas Online

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest

    steps:
      - name: Access Veritas Nexum
        run: |
          curl -L --fail --retry 3 "${{ secrets.VERITAS_URL }}"
